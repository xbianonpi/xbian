#
# Global configuration for XBian dev environment
#

#
# Build options
#
config_patch_fuzzy=5

#
# Options for building deb package
#
config_dpkg_deb_compat=8
config_dpkg_deb_opts=-Zxz
config_dpkg_deb_separator=_
config_dpkg_deb_addarch=yes

#
# Building packages for versions invoked by
# script build.preloader (Jenkins)
#
config_os_versions="stretch buster"

# Building for version, invoked by
# script $xbiangit
#
config_os_version=buster
