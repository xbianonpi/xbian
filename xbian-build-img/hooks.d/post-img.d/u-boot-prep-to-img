
[ $config_platform = imx6 ] || return 0


loopd=$(losetup -f)
losetup $loopd ./$IMGNAME

dd if=./SPL of=$loopd bs=1K seek=1 2>&1
dd if=./u-boot.img of=$loopd bs=1K seek=42 2>&1

losetup -d $loopd; sync
rm -f ./SPL ./u-boot.img
